@using SBS.Core.Models
@model IEnumerable<CityViewModel>

@{
    ViewData["Title"] = "Cities";
}

<h1>Cities</h1>

<p>
    <a asp-action="Create" class="btn btn-primary mt-2">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Country.Name)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Name)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Country.Name)
                </td>

                <td>
                    <a asp-controller="City" asp-action="Edit" asp-route-id="@item.Id" class="btn btn-warning">Edit</a>
                </td>
                <td>
                    <form asp-controller="City" asp-action="Delete" method="post">
                        <input name="id" type="hidden" value="@item.Id" />
                        <button class=" btn btn-danger" onclick="return confirm('Are you sure you want to delete atricle: @item.Name')">Delete</button>
                    </form>
                </td>
            </tr>
        }
    </tbody>
</table>
